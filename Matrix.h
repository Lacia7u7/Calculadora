#include "Vector.h"
#include "../../../../../Downloads/CNumber.h"
#include "CalculatorError.h"


class Matrix{
    private:
        Vector(Vector(CNumber));
    public:
        Matrix(int rows, int columns, class Vector(Vector(CNumber)));
        CNumber getDeterminant(class Vector(Vector(CNumber)));
};


#ifndef CALCULADORA_MATRIX_H
#define CALCULADORA_MATRIX_H

#endif //CALCULADORA_MATRIX_H
